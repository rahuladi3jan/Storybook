import { IconAlignment } from "./propTypes";

const IconAlignmentConfig: Record<IconAlignment, string> = {
  None: "none",
  LeftAligned: "left-aligned",
  RightAligned: "right-aligned",
};

export { IconAlignmentConfig };
