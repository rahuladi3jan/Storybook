export enum Direction {
  Top = "Top",
  Right = "Right",
  Bottom = "Bottom",
  Left = "Left",
}

export enum ToolTipAlignment {
  Start = "Start",
  Center = "Center",
  End = "End",
}

export enum BeakSize {
  Small = "Small",
  Medium = "Medium",
  Large = "Large",
  XL = "XL",
}

export enum RenderCondition {
  Hover = "Hover",
  Click = "Click",
}

export enum Theme {
  Light = "Light",
  Dark = "Dark",
}
