export enum Variant {
  Primary = "Primary",
  Secondary = "Secondary",
  Ghost = "Ghost",
}

export enum Size {
  Small = "Small",
  Medium = "Medium",
  Large = "Large",
  XL = "XL",
}

export enum Width {
  Auto = "Auto",
  Full = "Full",
  Half = "Half",
  Square = "Square",
  Circle = "Circle",
}

export enum IconAlignment {
  None = "None",
  LeftAligned = "LeftAligned",
  RightAligned = "RightAligned",
  Single = "Single",
}

export enum State {
  Enabled = "Enabled",
  Disabled = "Disabled",
}
