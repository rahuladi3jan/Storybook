import { Meta, Story } from "@storybook/addon-docs";
import { Source } from "@storybook/components";

import "./styles.scss";

export const PACKAGE_NAME = "@services/storybook-storybook";

export const Installation = () => {
  return (
    <div className="theme-container">
      <h5>Getting started</h5>
      <p>
        - You need to add an authToken in your .npmrc file of your current
        working project to get access for the installation of package.
      </p>
      <p>
        - To install `{PACKAGE_NAME}` in your project, you will need to run the
        following command using <a href="https://www.npmjs.com/">npm</a>:
      </p>
      <Source
        language="bash"
        dark={true}
        format={true}
        code={`npm install ${PACKAGE_NAME} --save-dev`}
      />
      <p>
        If you prefer <a href="https://yarnpkg.com/en/">yarn</a>, use the
        following command instead:
      </p>
      <Source
        language="bash"
        dark={true}
        format={true}
        code={`yarn add ${PACKAGE_NAME} --dev`}
      />
      <p>
        - Storybook UI component library can be consumed by all React / React with
        Typescript / NextJS etc projects.
      </p>
      <h5>Usage</h5>
      <p>
        The `{PACKAGE_NAME}` package provides components and icons for the
        Storybook design system.
      </p>
      <p>
        To use an atom component, you can import it directly from the{" "}
        {PACKAGE_NAME}/atoms package:
      </p>
      <Source
        language="js"
        dark={true}
        format={true}
        code={`import { Button } from '${PACKAGE_NAME}/atoms/index';
                function App() {
                    return <Button> Submit </Button>;
                }
                export default App;`}
      />
      <p>
        To use molecule component, you can import it directly from the{" "}
        {PACKAGE_NAME}/molecules package:
      </p>
      <Source
        language="js"
        dark={true}
        format={true}
        code={`
                import { MudraAccordion } from '${PACKAGE_NAME}/molecules/index';
                function App() {
                    return <MudraAccordion title="Accordion Title" className="basic"> Accordion Content </MudraAccordion>
                }
                export default App;
                `}
      />
      <p>
        To use icons, you can import it directly from the {PACKAGE_NAME}/icons
        package:
      </p>
      <Source
        language="js"
        dark={true}
        format={true}
        code={`
                import { Logout } from '${PACKAGE_NAME}/icons';
                function App() {
                    return <Logout />
                }
                export default App;
                `}
      />
      <p>For detailed examples, check out the storybook components</p>
      <br />
      <br />
      <br />
    </div>
  );
};

# Installation

<Story name="Installation">{Installation.bind({})}</Story>

<Meta
  title="Introduction/Installation"
  component={Installation}
  parameters={{
    options: { showPanel: false },
    previewTabs: {
      "storybook/docs/panel": {
        hidden: true,
      },
    },
  }}
/>
